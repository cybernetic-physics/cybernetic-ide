# must match the MuJoCo version the yoga policy was trained against
# (loco-mujoco / .venv-rl); solver behavior differs enough across versions
# that a mimic policy trained on one falls over on the other
mujoco==3.10.0
numpy
msgpack
websockets>=12,<16
pillow
