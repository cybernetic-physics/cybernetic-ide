"""Simulator-backed Unitree SDK2 compatibility package.

This package intentionally implements only the small high-level G1 surface
needed by Cybernetic IDE's current MuJoCo harness. It preserves the official
import shape so user code can later move to Unitree's real SDK2 transport with
minimal edits.
"""
